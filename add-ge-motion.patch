diff --git a/components/Playground.jsx b/components/Playground.jsx
--- a/components/Playground.jsx
+++ b/components/Playground.jsx
@@
-  const wordF=()=>{const s=buf[row]||"";let i=col+1;while(i<s.length&&/[A-Za-z0-9_]/.test(s[i]))i++;while(i<s.length&&/\W/.test(s[i]))i++;setCol(clamp(i));};
-  const wordB=()=>{const s=buf[row]||"";let i=Math.max(0,col-1);while(i>0&&/\W/.test(s[i]))i--;while(i>0&&/[A-Za-z0-9_]/.test(s[i-1]))i--;setCol(clamp(i));};
+  const wordF=()=>{const s=buf[row]||"";let i=col+1;while(i<s.length&&/[A-Za-z0-9_]/.test(s[i]))i++;while(i<s.length&&/\W/.test(s[i]))i++;setCol(clamp(i));};
+  const wordB=()=>{const s=buf[row]||"";let i=Math.max(0,col-1);while(i>0&&/\W/.test(s[i]))i--;while(i>0&&/[A-Za-z0-9_]/.test(s[i-1]))i--;setCol(clamp(i));};
+  // move to end of previous word (Vim 'ge')
+  const wordEndB=()=>{const s=buf[row]||"";let i=Math.max(0,col-1);
+    while(i>0&&/\W/.test(s[i]))i--; // skip whitespace left
+    if(/[A-Za-z0-9_]/.test(s[i])){   // inside a word: find its start then its end
+      let j=i; while(j>0&&/[A-Za-z0-9_]/.test(s[j-1])) j--;
+      let k=j; while(k+1<s.length&&/[A-Za-z0-9_]/.test(s[k+1])) k++;
+      i=k;
+    }
+    setCol(clamp(i));
+  };
@@
-    if(k==="w")return run(wordF,"w");
-    if(k==="b")return run(wordB,"b");
+    if(k==="w")return run(wordF,"w");
+    if(k==="b")return run(wordB,"b");
+    // 'e' or 'ge' depending on pending 'g'
+    if(k==="e"){ if(pending==="g"){ return run(wordEndB,"ge"); } return run(wordEndF,"e"); }
@@
-          Motion Playground <span className='text-slate-500'>(hjkl, 0, $, w, b, gg, G, x, u)</span>
+          Motion Playground <span className='text-slate-500'>(hjkl, 0, $, w, b, e, ge, gg, G, x, u)</span>
